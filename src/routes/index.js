'use strict';

const dashboardRoutes = require('./dashboard');
const jobOrderRoutes = require('./jobOrders');
const placeholderRoutes = require('./placeholders');

module.exports = {
  dashboardRoutes,
  jobOrderRoutes,
  placeholderRoutes,
};
